Use cWebView.pkg
Use cWebPanel.pkg
Use cWebMenuGroup.pkg
Use cWebMenuItem.pkg
Use cWebForm.pkg

Use cligaDataDictionary.dd
Use cequipoDataDictionary.dd
Use cjugadorDataDictionary.dd
Use SelectPosicion.wo   // APARTADO final: Select de posiciones para ZoomJugador.

Object oZoomJugador is a cWebView
    Set psCaption      to "ZoomJugador"
    Set piColumnCount  to 12

    Object oliga_DD is a cligaDataDictionary
    End_Object

    Object oequipo_DD is a cequipoDataDictionary
    End_Object

    Object ojugador_DD is a cjugadorDataDictionary
    End_Object

    Set Main_DD to ojugador_DD
    Set Server  to ojugador_DD

    Object oWebMainPanel is a cWebPanel
        Set piColumnCount to 12

        Object ojugadorNacionalidad is a cWebForm
            Entry_Item jugador.Nacionalidad
            Set piColumnSpan    to 8
            Set piColumnIndex   to 0
            Set peLabelPosition to lpTop
            Set psLabel         to "Nacionalidad"
        End_Object

        // Campo de Posición sin campo de tabla ligado (no existe jugador.Posicion)
        Object ojugadorPosicion is a cWebForm
            // Sin Entry_Item porque la tabla jugador no tiene columna Posicion
            Set piColumnSpan    to 4
            Set piColumnIndex   to 0
            Set peLabelPosition to lpTop
            Set psLabel         to "Posición"
            Set pbPromptButton  to True

            WebRegisterPath ntNavigateForward oSelectPosicion

            Procedure OnPrompt
                Send NavigatePath   // abre SelectPosicion como lookup de posiciones
            End_Procedure
        End_Object

        Object oequipoID is a cWebForm
            Entry_Item equipo.ID
            Set piColumnSpan    to 2
            Set piColumnIndex   to 0
            Set peLabelPosition to lpTop
            Set psLabel         to "EquipoID"
            Set pbPromptButton  to True

            Procedure OnPrompt
                Send NavigatePath
            End_Procedure
        End_Object

    End_Object  // oWebMainPanel

End_Object      // oZoomJugador