// C:\DataFlex Projects\FutbolWebApp\AppSrc\SelectPosicion.wo
// SelectPosicion
//

Use cWebView.pkg
Use cWebList.pkg
Use cWebMenuGroup.pkg
Use cWebMenuItem.pkg
Use cWebColumnButton.pkg
Use cWebColumn.pkg
Use ZoomPosicion.wo

Use cposicionDataDictionary.dd

Object oSelectPosicion is a cWebView
    Set psCaption       to "SelectPosicion"
    Set peWebViewStyle  to wvsDrilldown
    Set peViewType      to vtSelect
    Set piColumnCount   to 12
    Set pbShowCaption   to False
    Set piMaxWidth      to 1024

    Object oposicion_DD is a cposicionDataDictionary
    End_Object

    Set Main_DD to oposicion_DD
    Set Server  to oposicion_DD

    Object oList is a cWebList
        Set piColumnSpan       to 0
        Set psCSSClass         to "MobileList"
        Set pbServerOnRowClick to True
        Set pbFillHeight       to True
        Set pbShowHeader       to False
        Set piSortColumn       to 0

        Object oposicionID is a cWebColumn
            Entry_Item posicion.ID
            Set psCaption to "posicion.ID"
            Set piWidth   to 100
        End_Object

        Object oDetailButton is a cWebColumnButton
            Set psCaption      to "btn"
            Set piWidth        to 45
            Set pbFixedWidth   to True
            Set pbResizable    to False
            Set piListRowSpan  to 2
            Set psBtnCssClass  to "WebButtonIcon WebIcon_Info"
            Set peAlign        to alignRight

            WebRegisterPath ntNavigateForward oZoomPosicion

            Procedure OnClick
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bReadOnly
            End_Procedure
        End_Object

        Object oposicionNombre is a cWebColumn
            Entry_Item posicion.Nombre
            Set psCaption to "posicion.Nombre"
            Set piWidth   to 500
            Set pbNewLine to True
        End_Object

        // APARTADO final: Undefined -> Zoom jugador - Select posición.
        //
        // - Cuando SelectPosicion se abre desde ZoomJugador mediante el prompt,
        //   la navegación llega como nfUndefined o nfFromChild.
        // - En ambos casos, al hacer clic en una fila se cierra el Select y
        //   se devuelve el control al Zoom que lo abrió (ZoomJugador).
        // - No existe relación de clave foránea entre jugador y posición:
        //   solo usamos este Select como catálogo para rellenar el campo
        //   texto jugador.Posicion en ZoomJugador.
        Procedure OnRowClick String sRowId
            tWebNavigateData NavigateData
            Get GetNavigateData to NavigateData

            Case Begin
                Case (NavigateData.eNavigateType = nfFromParent)
                    // No se usa este flujo en el ejercicio.
                    Case Break

                Case (NavigateData.eNavigateType = nfFromChild)
                    Send NavigateClose Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromMain)
                    Send NavigateClose Self
                    Case Break

                Case Else // must be nfUndefined
                    // Desde ZoomJugador normalmente se llega como nfUndefined,
                    // así que también cerramos el Select tras elegir la posición.
                    Send NavigateClose Self
                    Case Break
            Case End
        End_Procedure

        Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
            Move True to NavigateData.bReadOnly
        End_Procedure

    End_Object

    Object oActionGroup is a cWebMenuGroup
 
    End_Object

End_Object